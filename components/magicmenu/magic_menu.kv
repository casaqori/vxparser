#:import Window kivy.core.window.Window
<MenuButton>
    pos_hint: {"center_x": .5}
    icon_size: "32sp"
    tooltip_text: root.icon.capitalize()
    tooltip_bg_color: 1, 1, 1, 1
    tooltip_text_color: 0, 0, 0, 1
    shift_y: self.height
    shift_right: dp(76)

<RMB@MenuButton>

<MagicMenu>
    size_hint: None, 1
    size: "64dp", Window.height
    pos_hint: {"center_x": .5, "center_y": .5}
    md_bg_color: "#202224"
    
    #canvas:
        #Color:
            #rgba: 0, 1, 0, .5 # root.color_marker
        #RoundedRectangle:
            #pos: self.x + dp(6), self.y + dp(6) + root.pos_marker
            #size: self.width - dp(12), self.width - dp(12)
            #radius: [(self.width - dp(12)) / 2]
    
    MDRelativeLayout:
        id: buttons_container
        
    
